/**
 * _isdigit - prints char
 * @c: char to print
 * Return: always 1
 */

int _isdigit(int c)
{
return (1);
}
/**
 * _strlen - prints char
 * @s: char to print
 * Return: always 1
 */

int _strlen(char *s)
{
return (1);
}
/**
 * _puts - prints char
 * @s: char to print
 * Return: always 1
 */

void _puts(char *s)
{
return (1);
}
/**
 * *_strcpy - prints char
 * @dest: char to print
 * @src: char to print
 * Return: always 1
 */

char *_strcpy(char *dest, char *src)
{
return (1);
}

/**
 * *_memset - prints char
 * @s: char to print
 * @b: char to print
 * @n: something
 * Return: always 1
 */
char *_memset(char *s, char b, unsigned int n)
{
return (1);
}
